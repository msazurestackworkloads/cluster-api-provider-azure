apiVersion: v1
kind: ConfigMap
metadata:
  name: manager-bootstrap-cloud-env
  namespace: system
data:
  cloud-env: ${AZURE_ENVIRONMENT}
  cloud-arm-endpoint: ${AZURE_ARM_ENDPOINT}
  identity-system: ${IDENTITY_SYSTEM}
